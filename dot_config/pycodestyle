[pycodestyle]
# Ignore specific error codes
ignore = E402, E501, E231, E131, E125, E121, E127, W503, E203

# Maximum line length (compatible with black formatter)
max-line-length = 88

# Exclude directories
exclude = 
    .git,
    __pycache__,
    .pytest_cache,
    .tox,
    .venv,
    venv,
    env,
    build,
    dist,
    *.egg-info
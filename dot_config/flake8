[flake8]
# Ignore specific error codes
ignore = E402, F403, W503, E203

# Maximum line length
max-line-length = 88

# Exclude directories
exclude = 
    .git,
    __pycache__,
    .pytest_cache,
    .tox,
    .venv,
    venv,
    env,
    build,
    dist,
    *.egg-info

# Maximum complexity
max-complexity = 10